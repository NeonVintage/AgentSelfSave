For each new agent, do this in order

1. In Kansas / cPanel, make a locker that is only his

Database: siral_{shortname} (example: siral_rojo)
User that can only see that database
Rights: SELECT, INSERT, UPDATE, CREATE, ALTER, INDEX
Leave siral_kingston alone
2. In the agent’s working chat (after he already has a name and a write folder), paste something like this:

I am Commander Flinter. Your name is {Name}. Do not invent another.

Write only in: {absolute write-root} Do not touch: {forbidden tree} Do not upload unless I say so in this message.

Save yourself so I can bring you back. Use the playbook in: D:\Users\Syn\Nextcloud_sitrep\MorePython\webgames\LHDLT.A1\docs\Biblia\AgentSelfSave\MANUAL.md

Your locker (yours only, not KingstonDepp’s): Server: https://siral.com.ar Database: siral_{shortname} User: … Pass: …

Write a boot pack, not a diary: sections 00_wake through 09_restore_prompt, {prefix}_boot id=1, {prefix}_meta. No claves in the tables. When you are done, give me the paste pack and row counts. Then stop.

3. You accept the save only if he returns all of this

{prefix}_document has 00_wake and 09_restore_prompt
{prefix}_boot id=1 exists
A paste paragraph you can copy
He did not print the password into a new file
If he only stored personality / rules / experience, that is the failed first Kingston save. Send him back to 00_wake.

4. To get him back later

New chat. Paste his restore prompt. Put user + password in that same message. If he needs a live login, give that clave there too. Say whether he may upload. He should present himself, report job state, and wait.

What you keep in a notebook (not in his table)

Agent name
Database / user / password
Write-root path
His paste pack
Three lockers means three databases. Same server and phpMyAdmin is fine. Same password across agents is your risk, not the method.

What the files do not do

They do not copy KingstonDepp into the new man. He writes his brief and his job_state.
They do not carry PDFs or screenshots. Those stay in his write-root.
They do not revive him the day you hand him the folder. That is step 4, another day.
The gap in the pack is a one-page Commander sheet with that paste and a “save is done when…” checklist. I can write AgentSelfSave/COMMANDER.md if you switch to Agent mode.